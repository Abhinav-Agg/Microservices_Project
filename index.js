// require("dotenv").config();
// const express = require("express");
// const app = express();
// const cors = require("cors");
// const { connectDB } = require("./common/db");

// connectDB();

// app.use(cors());

// app.listen(process.env.PORT, () => {
//     console.log("Server is listening at mentioned PORT", process.env.PORT);
// });